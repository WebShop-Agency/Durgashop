modules = ["nodejs-24"]

[deployment]
router = "application"
deploymentTarget = "autoscale"

[deployment.postBuild]
args = ["npm", "cache", "clean", "--force"]
env = { "CI" = "true" }

[workflows]
runButton = "Project"

[agent]
stack = "NPM_WORKSPACE"
expertMode = true

[nix]
packages = ["zip"]
